<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ✅ Spring Boot 기본 콘솔 스타일 유지 -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- ✅ RecommendationController 로그 파일 (20분 단위처럼 분할) -->
    <appender name="RECOMMENDATIONCONTROLLER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/recommendation-service/recommendationController.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/recommendation-service/recommendationController.%d{yyyy-MM-dd_HH-mm}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ✅ 루트 로거: 콘솔 출력 유지 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- ✅ RecommendationController 전용 로거 -->
    <logger level="INFO"
      name="edu.goorm.recommendationservice.domain.controller.RecommendationController"
      additivity="false">
        <appender-ref ref="RECOMMENDATIONCONTROLLER"/>
    </logger>

</configuration>
